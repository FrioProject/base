{
  "name": "@local/base",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "applications/*",
    "development/*"
  ],
  "scripts": {
    "list:workspaces": "npm ls --workspaces --depth=0",
    "dev:registry:start": "npm run start -w=development/registry",
    "dev:core:build": "npm run build -w=packages/core"
  }
}
